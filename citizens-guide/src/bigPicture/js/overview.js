import { select, event } from 'd3-selection';
import { scaleLinear, scaleTime } from 'd3-scale';
import { extent, max } from 'd3-array';

const d3 = { select, event, scaleLinear, scaleTime, extent, max };